   0 date
   1 data +%D
   2 date +%D
   3 date +%d-%m-%y
   4 date +%d-%m-%y > hw4.txt
   5 tree
   6 df
   7 df -h
   8 df -h | awk NR=2
   9 df -h | awk NR==2
  10 df -h | grep -w /
  11 df -h | grep -w / >> hw4.txt
  12 cat hw4.txt
  13 history
  14 history | tail -3 >> hw4.txt
  15 cat hw4.txt
  16 cat /etc/passwd | grep cron
  

Записать в новый файл дату.
Дописать в этот файл информацию о занятом пространстве на корневом разделе файловой
системы (/)
3. Дописать в файл три последних команд из истории команд.
4. Дописать в файл информацию из (/etc/passwd), но отфильтруйте информацию так, чтобы в
файл ушли только строки со словом cron
5. 6. Дописать в файл количество строк со словом root из (/etc/group)
В текстовом редакторе добавьте второй строкой фразу “Modified with text editor”
Экспортируйте и пришлите результирующий файл в формате txt при помощи команды export_fileale



